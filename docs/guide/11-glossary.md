# 11 术语表

> 按主题分组。每条给一句话定义 + 指向详细解释的篇目。

## 物理与离散化

| 术语 | 一句话 | 详见 |
| --- | --- | --- |
| CSEM / 可控源电磁 | 用人工电流源激励的频域电磁勘探（区别于你做过的天然场 MT） | 01 |
| 时谐约定 | 场随时间按 e^{+iωt} 还是 e^{-iωt} 振荡的选择，决定虚部符号 | 01, 03 |
| σ（电导率） | 介质导电能力，本问题中跨 10 个数量级的罪魁 | 01 |
| curl-curl 算子 | ∇×(μ⁻¹∇×·)，电磁刚度项；零空间是全体梯度场 | 01 |
| Kr / Ki | 系统实部（curl-curl 刚度阵）/ 虚部（σ 加权质量阵） | 01 |
| Nédélec 棱边元 | 自由度挂在网格棱上的矢量有限元，保切向连续 | 02 |
| Whitney 基 / 环量自由度 | 自由度 = 场沿边的线积分（本代码约定） | 02 |
| 单位切向归一化基 | 自由度 = 边上切向场值 = 环量/边长（对方约定） | 02 |
| 离散梯度矩阵 G | 边数×节点数的 ±1 关联矩阵，把节点势映到边环量 | 02 |
| Kr·G = 0 | "梯度场是 curl-curl 零空间"的矩阵形式，本案最强探针 | 02, 06 |
| 规范自由度 / gauge | 方程约束不到的解分量；空气区梯度模态 | 08 |

## 线性代数与求解器

| 术语 | 一句话 | 详见 |
| --- | --- | --- |
| CSR | 行指针+列号+值的稀疏矩阵压缩格式（你 Fortran 项目用过的那个） | - |
| 1 基 / 0 基 | Fortran 数组下标从 1 起 / C 从 0 起；adapter 负责转换 | 04 |
| Krylov 方法 | CG/GMRES 等"只靠矩阵乘向量"的迭代法家族 | 04 |
| KSP / PC | PETSc 里迭代求解器 / 预条件器的对象名 | 04 |
| GMRES / FGMRES | 通用 Krylov 法；F 版允许预条件器每步变化（本项目外层） | 04 |
| CG / FCG | 对称正定专用 Krylov 法；F 版允许变预条件器。FCG 在本项目有浮点误报坑 | 05 |
| MatShell | PETSc 的"无矩阵"算子：只提供乘向量回调 | 04 |
| 预条件器 | 用"近似求逆"加速迭代收敛的变换；不改变解，只改变快慢 | 04, 05 |
| 块对角预条件 | 对 2×2 块系统用 diag(B⁻¹,B⁻¹) 近似；本项目外层方案 | 04 |
| B = C + M | 内层预条件矩阵 = Kr + |Ki|，对称正定 | 03, 04 |
| AMG / BoomerAMG | 代数多重网格（Hypre 的实现）；解拉普拉斯类矩阵的最优方法 | 05 |
| AMS | Hypre 的 curl-curl 专用预条件器：拆出梯度/矢量两个辅助空间分别交给 AMG | 05 |
| α 空间 / β 空间 | AMS 的两个辅助空间：矢量节点场（Π）/ 标量节点势（G） | 05 |
| Π（Pi 插值阵） | AMS 内部由 G 和节点坐标自动构造的节点→边插值 | 05 |
| β Poisson 矩阵 | Gᵀ·B·G，σ 加权节点拉普拉斯；修复 2 改为手工构造 Gᵀ·M·G | 05, 07 |
| 跳系数问题 | 矩阵系数跨多个量级的拉普拉斯问题，AMG 的难点 | 07 |
| happy breakdown | GMRES 遇到 A·z=0 时的退化"收敛"；本案中是全零解假象的机制 | 06 |
| DIVERGED_NANORINF / _INDEFINITE_MAT / _ITS | PETSc 收敛失败代码：出现 NaN / 曲率非正 / 步数用尽 | 05, 06 |
| 条件数 | λmax/λmin，迭代法收敛难度的度量；本问题裸奔 ~1e12 | 05 |
| MatPtAP | PETSc 的三连乘 PᵀAP 接口，用来算 GᵀMG | 07 |
| MatShift | 给矩阵对角加常数；修复 3 的实现 | 07 |

## 项目专有

| 术语 | 一句话 | 详见 |
| --- | --- | --- |
| eg_1 | 仓库自带 3,124 边小算例，全程黄金回归基准 | 00 |
| 黄金哈希 | eg_1 结果的 sha256 `342042...d266`，默认路径不变性的证明 | 10 |
| ams_example | 对方 682,344 边大算例（上三角 CSR + 网格 + rhs + 参考解） | 00 |
| slove.txt | 对方直接求解器的参考解（注意拼写就是 slove，对方笔误传统） | 00, 08 |
| adapter | 把对方格式（1 基/上三角/物理符号/归一化基）翻成内部格式的接口层 | 04 |
| dual mode | 代码把同一系统求解两遍的历史遗留结构，待优化 | 04, 10 |
| 参考残差 | slove.txt 代回方程的残差（6.8e-7），数据自洽性指标 | 10 |
| UpperDumpSystem / AmsLab | 二进制转储 + 秒级试参实验台（本轮新增） | 10 |
| 三层修复 | 反缩放 / 干净 β / 对角位移，对应三个 opt-in 选项 | 07 |
